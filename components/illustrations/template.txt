import React from 'react';
import { Svg, Path } from 'react-native-svg';

const [name] = ({ width, height }: { width: number; height: number }) => {
    return (
        
    );
}

export default [name];